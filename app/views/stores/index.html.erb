<h1 class="title is-1">Stores</h1>
<table class="table is-striped is-hoverable">
  <tr>
    <th>Name</th>
    <th>Number of products</th>
  </tr>
 
  <% @stores.each do |store| %>
    <tr>
      <td><%= link_to store.name, store_path(store) %></td>
      <td><%= store.products.length %></td>
    </tr>
  <% end %>
</table>